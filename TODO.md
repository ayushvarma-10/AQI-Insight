# TODO List for AQI Project

- [x] Create `requirements.txt` with necessary Python dependencies (streamlit, requests, pandas, plotly, etc.)
- [x] Create `data_fetch.py` module for API calls to fetch AQI and weather data (using WAQI for AQI and OpenWeatherMap for weather)
- [x] Create `analysis.py` module for data processing, trends, and visualizations (using Pandas and Plotly)
- [x] Create `app.py` main Streamlit application with search functionality, data display, and charts
- [x] Create `README.md` with setup instructions, API key requirements, and usage guide
- [x] Install dependencies using `pip install -r requirements.txt`
- [x] Run and test the Streamlit app with `streamlit run app.py`
- [x] Verify search functionality, data display, and visualizations; obtain API keys if needed
